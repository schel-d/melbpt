extends page-template/page-template.pug

block head
  script.
    window.stopID = #{id};
  +head-content(
    "Home",
    "/css/stop.css",
    "/js/stop.js",
    `Lookup train departures at ${ name }.`)

block body
  .page-centered
    h1= name
    #departures
